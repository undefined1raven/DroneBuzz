
const EnergyWeaponConfig = () => {
    const config = {
        radius: 0.00808,
        powerScale: 1,
        duration: 10000,
        cost: 1,
    }
    return config;
}

export default EnergyWeaponConfig;