
const EnergyWeaponConfig = () => {
    const config = {
        killDurationRange: [300, 2000],
        radius: 0.00808,
        duration: 100,
        cost: 1,
    }
    return config;
}

export default EnergyWeaponConfig;